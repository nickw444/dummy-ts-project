{
  "name": "ts-repo-test",
  "version": "1.0.0",
  "description": "Demo TypeScript project for testing IDE refactoring tools",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:single": "tsc -p tsconfig.single.json",
    "typecheck:projects": "tsc -p tsconfig.projects.json"
  },
  "keywords": ["typescript", "demo", "refactoring"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
